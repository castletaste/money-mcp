# mcp-money: Архитектурный документ

## Итоговые решения

| Аспект | Решение |
|--------|---------|
| **Позиционирование** | Трекер расходов для AI-ассистентов |
| **Объём MVP** | Транзакции + категории + теги + сводки + базовые бюджеты (16 инструментов). Без счетов. |
| **Рантайм** | Основной — Bun, совместим с Node (без Bun-специфичных API в продакшн-коде) |
| **MCP транспорт** | Только stdio через `@modelcontextprotocol/sdk` |
| **База данных** | Только PostgreSQL, через Drizzle ORM + postgres.js |
| **Схема** | 5 таблиц: categories, tags, transactions, transaction_tags, budgets. Всё в схеме `mcp_money`. |
| **Миграции** | Автомиграция при запуске с отслеживанием версий |
| **Конфигурация** | `DATABASE_URL` обязателен. `MCP_MONEY_SCHEMA` — опциональная env-переменная. Валюта меняется через инструмент `set_currency`. |
| **Тестирование** | Интеграционные тесты + E2E MCP-тесты через `bun test` |
| **Наблюдаемость** | Логирование в stderr + логирование SQL-запросов (DEBUG) + инструмент `health_check` |
| **Безопасность** | Изоляция через схему + параметризованные запросы |
| **Упаковка** | npm-пакет, запуск через `npx mcp-money` |
| **Open-source** | Один мейнтейнер, качество референсной реализации |
| **Запуск** | Каталоги MCP → Reddit → HN |
| **Название** | `mcp-money` |

---

## Схема БД

```
mcp_money.categories
  id          uuid PK
  name        text NOT NULL UNIQUE
  parent_id   uuid FK -> categories(id)  -- максимум один уровень вложенности
  type        text NOT NULL  -- 'expense', 'income'
  created_at  timestamptz

mcp_money.tags
  id          uuid PK
  name        text NOT NULL UNIQUE
  created_at  timestamptz

mcp_money.transactions
  id          uuid PK
  category_id uuid FK -> categories(id) NULLABLE
  amount      numeric(19,4) NOT NULL  -- положительное = доход, отрицательное = расход
  currency    text NOT NULL DEFAULT 'USD'
  description text
  date        date NOT NULL
  metadata    jsonb DEFAULT '{}'
  created_at  timestamptz
  updated_at  timestamptz

mcp_money.transaction_tags
  transaction_id  uuid FK -> transactions(id) ON DELETE CASCADE
  tag_id          uuid FK -> tags(id) ON DELETE CASCADE
  PRIMARY KEY (transaction_id, tag_id)

mcp_money.budgets
  id          uuid PK
  category_id uuid FK -> categories(id)
  amount      numeric(19,4) NOT NULL
  period      text NOT NULL  -- 'monthly', 'weekly'
  start_date  date NOT NULL
  created_at  timestamptz
```

**Соглашения**:
- `numeric(19,4)` для денег, никогда float
- UUID v7 для сортируемых по времени ID
- `date`, а не `timestamp` для дат транзакций
- Отрицательные суммы = расход, положительные = доход. MCP-слой обрабатывает `add_expense(amount: 50)` → сохраняет как `-50`.
- Колонка валюты на транзакциях оставлена для будущей мультивалютности, конвертация в v1 не реализуется.

---

## MCP-инструменты (16)

| Инструмент | Назначение |
|------------|------------|
| `add_transaction` | Записать расход или доход (принимает опциональные теги) |
| `list_transactions` | Запрос с фильтрами (диапазон дат, категория, тег, лимит) |
| `update_transaction` | Редактировать транзакцию (включая теги) |
| `delete_transaction` | Удалить транзакцию |
| `get_summary` | Расходы по категориям за период, итоги, средние |
| `list_categories` | Показать доступные категории |
| `create_category` | Добавить пользовательскую категорию |
| `delete_category` | Удалить категорию (ошибка, если есть ссылающиеся транзакции) |
| `list_tags` | Показать все теги |
| `create_tag` | Добавить новый тег |
| `delete_tag` | Удалить тег (удаляет из всех транзакций) |
| `set_budget` | Установить месячный бюджет для категории |
| `get_budget_status` | Проверить бюджет vs фактические траты |
| `delete_budget` | Удалить бюджет |
| `set_currency` | Сменить валюту по умолчанию для новых транзакций |
| `health_check` | Статус БД, версия, статистика |

**Поведение деструктивных инструментов**:
- `delete_transaction` — жёсткое удаление
- `delete_category` — RESTRICT: ошибка, если есть транзакции с этой категорией
- `delete_tag` — CASCADE: удаляет тег и все связи в transaction_tags, транзакции не затрагиваются
- `delete_budget` — жёсткое удаление

---

## Бизнес-логика

**Категории**: ~10 предустановленных (Groceries, Restaurants, Transport, Entertainment, Utilities, Rent, Salary, Healthcare, Shopping, Other). Пользователи добавляют свои через `create_category`. Максимум один уровень вложенности (parent_id), в v1 не форсируется.

**Теги**: Гибкие сквозные метки. Many-to-many через связующую таблицу. Создаются на лету в `add_transaction` или через `create_tag`. Без предустановок — полностью пользовательские.

**Бюджеты**: Только месячные по категории. Покрывает 90% сценариев.

**Валюта**: Меняется через MCP-инструмент `set_currency`. По умолчанию: `USD`. Хранится в строке key-value `mcp_money.settings`. Валюта на каждой транзакции, без конвертации — сводки группируются по валюте.

**Миграции**: Автомиграция при запуске. Таблица версий `mcp_money.schema_version`. Применяется в транзакции, откат при ошибке. Деструктивные миграции требуют флага `--force`.

---

## Конфигурация

```json
{
  "mcpServers": {
    "money": {
      "command": "npx",
      "args": ["mcp-money"],
      "env": {
        "DATABASE_URL": "postgresql://user:pass@localhost:5432/mydb"
      }
    }
  }
}
```

- `DATABASE_URL` — обязателен
- `MCP_MONEY_SCHEMA` — опционален (по умолчанию `mcp_money`)
- `DEBUG=true` — включает логирование SQL-запросов в stderr

---

## План реализации

### Фаза 0: Инициализация репозитория (День 1)

- package.json с name, version, description, bin, exports
- Зависимости: `@modelcontextprotocol/sdk`, `drizzle-orm`, `postgres`
- Dev-зависимости: `drizzle-kit`, `@types/bun`, `typescript`
- tsconfig.json для совместимости Bun + Node
- `.github/workflows/ci.yml` (lint + тесты)
- docker-compose.yml с Postgres
- .gitignore, LICENSE (MIT)

### Фаза 1: Слой базы данных (День 2-3)

- Drizzle-схема в `src/db/schema.ts` (5 таблиц)
- Система автомиграций с отслеживанием версий
- Подключение к БД через `DATABASE_URL`
- Сид ~10 дефолтных категорий
- Интеграционные тесты для создания схемы и сидинга

### Фаза 2: Скелет MCP-сервера (День 3-4)

- MCP-сервер с `@modelcontextprotocol/sdk` + `StdioServerTransport`
- Регистрация инструментов с JSON-схемами
- Инструмент health_check
- Жизненный цикл подключения к БД (connect при старте, close при выходе)

### Фаза 3: Основные инструменты (День 4-7)

Все 16 инструментов:
- `add_transaction`, `list_transactions`, `update_transaction`, `delete_transaction`
- `get_summary`
- `list_categories`, `create_category`, `delete_category`
- `list_tags`, `create_tag`, `delete_tag`
- `set_budget`, `get_budget_status`, `delete_budget`
- `set_currency`, `health_check`
- Интеграционные тесты + E2E MCP-тесты

### Фаза 4: Полировка и релиз (День 8-10)

- Настройка npm-пакета (bin entry, exports)
- Шаг сборки для совместимости с Node.js
- README: питч, установка, конфигурация, примеры использования, демо GIF
- CHANGELOG.md, CONTRIBUTING.md
- Тестирование на Claude Desktop, Claude Code, Cursor
- Публикация в npm, подача в awesome-mcp-servers

### Фаза 5: После MVP (Месяц 2+)

По запросам пользователей:
- Поддержка счетов (опциональный FK на транзакциях)
- Повторяющиеся транзакции
- CSV импорт/экспорт
- Тренды расходов
- Мультивалютность с конвертацией
- Поддержка SQLite через Drizzle

---

## Структура репозитория

```
mcp-money/
├── src/
│   ├── index.ts              # точка входа: парсинг конфига, подключение к БД, запуск MCP-сервера
│   ├── config.ts             # парсинг и валидация env-переменных
│   ├── db/
│   │   ├── connection.ts     # подключение postgres.js
│   │   ├── schema.ts         # определения таблиц Drizzle (5 таблиц)
│   │   ├── migrate.ts        # раннер автомиграций
│   │   ├── seed.ts           # дефолтные категории (~10)
│   │   └── migrations/       # нумерованные SQL-файлы
│   │       ├── 001_initial.sql
│   │       └── ...
│   └── tools/
│       ├── index.ts          # реестр инструментов
│       ├── transactions.ts   # add, list, update, delete
│       ├── categories.ts     # list, create, delete
│       ├── tags.ts           # list, create, delete
│       ├── budgets.ts        # set, get_status, delete
│       ├── summary.ts        # get_summary
│       ├── settings.ts       # set_currency
│       └── health.ts         # health_check
├── tests/
│   ├── setup.ts              # настройка/очистка тестовой БД
│   ├── transactions.test.ts
│   ├── budgets.test.ts
│   ├── summary.test.ts
│   └── e2e/
│       └── mcp-protocol.test.ts
├── package.json
├── tsconfig.json
├── drizzle.config.ts
├── docker-compose.yml
├── LICENSE
├── README.md
├── CHANGELOG.md
└── CLAUDE.md
```

---

## Журнал решений

### Принято

| Решение | Обоснование |
|---------|-------------|
| Только PostgreSQL | SQL-first требование, без локальных файлов, Drizzle даёт типобезопасность |
| Drizzle ORM | Типобезопасный, готов к нескольким диалектам в будущем, лёгкий |
| Драйвер postgres.js | Работает и на Bun, и на Node, без нативных биндингов |
| MCP только через stdio | 95% использования MCP — stdio, HTTP преждевременен |
| Автомиграция при запуске | Нулевое трение для пользователей, критично для adoption |
| Без счетов в MVP | Упрощает схему и инструменты. Можно добавить позже как опциональный FK. |
| Теги в MVP | Лёгкий many-to-many. Гибкая маркировка помимо жёстких категорий. |
| 16 инструментов | CRUD для всех сущностей + сводка + статус бюджета + set_currency + health_check |
| Отрицательные суммы для расходов | Проще математика, LLM занимается отображением |
| UUID v7 для ID | Сортируемые по времени, без конфликтов последовательностей |
| Схема `mcp_money` | Изоляция без необходимости выделенной БД |
| Дистрибуция через npm | Стандартный паттерн установки MCP |
| Только месячные бюджеты | 90% случаев, простая реализация |
| ~10 предустановленных категорий | Лучший UX, чем пустой старт, но не перегружает |
| `numeric(19,4)` для денег | Никогда float. Стандартная точность индустрии. |
| Валюта через MCP-инструмент | Естественнее, чем env-переменная — LLM говорит «переключи на EUR» |
| Интеграционные + E2E MCP-тесты | Полное покрытие: логика инструментов + соответствие протоколу |
| Колонка валюты на транзакциях | Оставлена для будущей мультивалютности |

### Сознательно отвергнуто

| Решение | Причина |
|---------|---------|
| SQLite как основное хранилище | Требование: без локальных файлов |
| Таблица счетов в MVP | Добавляет сложность без пропорциональной пользы для v1 |
| Поддержка нескольких БД в v1 | Взрывная сложность при недоказанном спросе |
| Двойная бухгалтерия | Пугает пользователей, решает неправильную задачу для личного трекинга |
| Конфигурационный файл | 2 настройки не оправдывают конфиг-файл |
| Docker для MCP-сервера | stdio через Docker — неудобно |
| Rate limiting | Абсурд для инструмента личных финансов |
| MCP-подтверждение для удалений | У MCP нет нативного механизма подтверждения |
| Структурное логирование (pino) | Переусложнено для v1 |
| Event sourcing | 10x сложности при почти нулевой пользе в личных финансах |
| Система плагинов | Преждевременная абстракция |
| Валюта как env-переменная | Инструментный подход естественнее для взаимодействия с LLM |

### Отложено до пост-MVP

| Фича | Когда | Триггер |
|------|-------|---------|
| Счета | v1.1 | Запрос пользователей на разделение наличных/банк/карта |
| Повторяющиеся транзакции | v1.1 | Запрос пользователей |
| Мультивалютная конвертация | v1.2 | Запрос + найден хороший API курсов |
| CSV импорт/экспорт | v1.1 | Очевидный ранний запрос |
| Тренды расходов | v1.2 | Когда сводки докажут полезность |
| Поддержка SQLite | v2.0 | Если Postgres блокирует adoption |
| HTTP/SSE транспорт | v2.0 | Когда MCP-клиенты массово поддержат |
| Веб-дашборд | Никогда (вероятно) | Вне скоупа MCP-сервера |

### Пересмотреть после MVP

| Решение | Пересмотреть когда |
|---------|-------------------|
| Только PostgreSQL | Если >30% issues — «как настроить Postgres» |
| Без счетов | Если >5 пользователей просят разделение транзакций по источнику |
| Конвенция знака суммы | Если пользователи/LLM путаются в отрицательных суммах |
| Автомиграция | Если хоть один пользователь сообщит о потере данных |
| Drizzle ORM | Если вызывает проблемы на Node в рантайме |
| Количество инструментов | Если LLM не справляются (уменьшить) или пользователи просят больше (расширить) |
| Только месячные бюджеты | Если >5 запросов на недельные/квартальные |
| UUID v7 | Если проблемы совместимости с драйверами |
